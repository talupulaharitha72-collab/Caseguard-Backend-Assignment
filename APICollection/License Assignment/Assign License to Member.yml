info:
  name: Assign License to Member
  type: http
  seq: 1

http:
  method: post
  url: http://localhost:5100/api/organizations/{{org_id}}/members/{{user2_id}}/license
  headers:
    - name: Authorization
      value: Bearer {{user1_token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
