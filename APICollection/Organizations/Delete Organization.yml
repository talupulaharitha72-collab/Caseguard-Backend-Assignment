info:
  name: Delete Organization
  type: http
  seq: 6

http:
  method: delete
  url: http://localhost:5100/api/organizations/{{org_id}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
