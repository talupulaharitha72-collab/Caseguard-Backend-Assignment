info:
  name: Get Single Member
  type: http
  seq: 3

http:
  method: get
  url: http://localhost:5100/api/organizations/{{org_id}}/members/{{user2_id}}
  headers:
    - name: Authorization
      value: Bearer {{user1_token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
