info:
  name: Accept Invitation
  type: http
  seq: 6

http:
  method: post
  url: http://localhost:5100/api/invitations/{{invitation_id}}/accept
  headers:
    - name: Authorization
      value: Bearer {{user2_token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
