namespace CaseGuard.Backend.Assignment.Models;

public enum OrganizationRole { Owner = 1, Admin = 2, Member = 3 }
public enum InvitationStatus { Pending = 1, Accepted = 2, Cancelled = 3 }
public enum LicenseStatus { Active = 1, Expired = 2, Revoked = 3 }
